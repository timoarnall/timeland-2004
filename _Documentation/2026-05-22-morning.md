# 2026-05-22 — 2004 - Timeland

## Yesterday's calls

- **Q1 (scope — A/B/C).** Reading C now realized as a clickable atlas page on its own branch, matching B from two nights back. All three readings are now real pages, not sketches. Still on the table for the call itself.
- **Q2 (latitude-aware projection).** No movement. Preview tool and renderer flag both still on their branches.
- **Q3 (combine three additive yeses).** No movement. Still one fast-forward.
- **Q4 (scope front door).** Done in spirit — B and C both exist as working pages now.

## What's happening

The working branch took only briefing commits. All the motion is in the engine wing's scope question, and that question has reached the point it was built toward: every one of the three readings now exists as a page you can land on and click through, not a mockup or a file-move list. A is the live root, B and C are realized on branches.

## Direction and drift

The piece is the faithful 2026 port of the 2004 Iceland work. The engine that plays other trips is the drift, and the scope call decides whether it stays a side folder, sits below the archive with a sign, or takes the front (readings A / B / C). Nothing more needs writing about it. The only thing left was a way to feel all three against each other in one sitting.

## Open questions

1. **Scope — A archive / B both with signage / C atlas.** Materials are complete; tonight adds the comparison itself.
   - `git checkout overnight/2026-05-22-scope-decide-switcher` and open `_scope_decide.html`. Three buttons, one frame, keys 1/2/3. Land on each, click in, pick the one you'd want a stranger to meet first.
   - If one wins cleanly, its file becomes `index.html` (A is already that).
2. **Latitude-aware projection.** Still one browser-open away on its branch. Decision pending: default-off flag or drop it.
3. **Land the three additive yeses as one.** Perf-audit close, speculation README, CLAUDE.md draft — still a single fast-forward.

## Speculative work tonight

- `overnight/2026-05-22-scope-decide-switcher` — `_scope_decide.html`, the three front doors A/B/C in one switcher with keyboard nav. Pulls B and C onto the same branch so one checkout shows all three. Reversible: `git branch -D`.

## Still on the table

- `overnight/2026-05-21-reading-c-realized` — reading C as the atlas page.
- `overnight/2026-05-20-reading-b-realized` — reading B as the signed front door.
- `overnight/2026-05-19-scope-decision-package` — four scope docs plus the matrix.
- `overnight/2026-05-18-scope-landing-mockups` — three static landing mockups.
- `overnight/2026-05-17-scope-sketches` — trees and `git mv` lists for A, B, C.
- `overnight/2026-05-16-scope-question`, `overnight/2026-05-16-projection-preview`.
- `overnight/2026-05-15-combine-yeses`, `overnight/2026-05-15-speculation-index`.
- `overnight/2026-05-14-projection-latitude-aware`, `-perf-audit-close`, `-speculation-readme`, `-claude-md`.

## Details

- HEAD on `experiment/2026-update`: `9b143db` before tonight.
- New branch: `overnight/2026-05-22-scope-decide-switcher`, pushed to origin. New file `_scope_decide.html` at repo root (~150 lines, single file, no build). B and C files (`_scope_reading_b_signage.html`, `_scope_reading_c_atlas.html`) pulled onto the branch so the switcher can load all three from one checkout.
- Reading C branch tip before tonight: `fd119b8`. Reading B branch: `830b1e6`.
- Switcher style copied from the 05-18/05-20 pages: mono, warm grey panel, red accent, two type sizes.
- 13 overnight branches now. Last engine code edit: `_speculation_other_datasets/render.html`, 2026-05-16. Last preservation code change: `experiment-2026/index.html`, commit `ceb97a1`, 2026-05-11.
- `_Documentation/todo_unfix_bugs.md`: both items remain DONE. No new TODO/`?` markers outside `_archive_2004`.
