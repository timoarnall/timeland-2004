# 2026-05-12 — 2004 - Timeland

Working tree is dirty (the speculation folder is still untracked and now larger), so research-only mode: no speculative branches tonight.

## Yesterday's calls

The five questions from yesterday, graded against today's commits:

- **Q1 (speculation folder: sub-project, sibling, or parked).** Ignored. The folder grew further inside this repo — two more datasets materialised, plus an elevation helper. The drift is now louder than the question.
- **Q2 (1200-vs-241 as a road not taken).** Ignored. No README note in `experiment-2026/`.
- **Q3 (ImageBitmap revert as a closed door).** Ignored. Perf audit doc not updated.
- **Q4 (+15% marker bump: lift or revert).** Ignored. Still sitting between annotated and explained.
- **Q5 (CLAUDE.md at root).** Ignored. Still absent.

None of the five landed. One adjacent thing did: another perf pass on the main piece closed out the last three sub-cap seeks with an occlusion-stack cull — a real engineering win, but on a different question than any of the five.

## What's happening

Two threads today. The first was a clean perf landing on the main port: an occlusion-stack cull that handles the Reykjavik and Geysir photo clusters by skipping draws when a newer same-cluster photo is fully opaque. Every seek sample now hits the render cap; the sidebyside recording was refreshed to match. The second thread was the speculation folder growing again — two more datasets prototyped to render-ready state, an elevation lookup script added, and screenshots accumulating in a scratch folder.

## Direction and drift

Drift, second day. The speculation folder went from one materialised dataset yesterday to three today, with segments and screenshots and a findings note. The repo is now visibly carrying two missions: preserving the 2004 piece (shipped, embedded, quiet) and generalising the engine into a multi-trip renderer (active, growing, untracked). The longer this stays untracked, the more it functions as deniable side-work rather than an explicit choice.

## Open questions

1. **The speculation folder is no longer speculative.** It has three datasets, a builder, a forked renderer, and segmented checkpoints. Either commit it (here, or move it out) or quarantine it.
   - a. Commit in place. Lineage honest, mission widens. One-line `_speculation_other_datasets/README.md` says "tool spun out of the renderer, not Timeland canon".
   - b. `git mv` to a sibling repo today. Cleaner boundary. Cost: one evening of disentangling.
   - c. Quarantine: rename to `_attic/` with a README dated today saying "parked, not the Timeland mission". Commits the work to history without endorsing the pivot.
2. **All five of yesterday's questions are now ≥ 2 days dormant.** Worth deciding whether that pattern is signal (the engine and the preservation are both done, nothing left to call) or noise (the drift is consuming attention that should be closing out the smaller items).
3. **Occlusion cull as the closing perf chapter.** Today's commit closed the last sub-cap seeks. Worth a one-paragraph addition to `experiment-2026-perf-audit.md` so the perf story has a clean end, and a future session doesn't reopen perf as if it were still open.

## Speculative work tonight

None. Dirty tree.

## Stale speculative branches

None.

## Details

- Today's commits on `experiment/2026-update`: `540e341` occlusion-stack cull; `ceb97a1` refresh sidebyside recording. HEAD: `ceb97a1`. Branch is 18 commits ahead of `main` (was 10 yesterday).
- Occlusion cull mechanism: `CLUSTER_R=1` design unit, per-photo next-in-cluster linked list, `drawPhotos` skips when a newer same-cluster photo has `timer >= 100`. Closed the three remaining sub-cap seeks (0.25 → 29.88 fps, 0.45 → 29.13 fps, 0.50 → 29.75 fps) to render cap.
- Speculation folder additions since yesterday: `FINDINGS.md` (60 lines), `lookup_elevation.py`, `norway-weekend-2013/` (manifest + 4 segment renders), `summer-2010/` (manifest + 6 segment renders); `bkk-burma-2012/` now has 4 segments. New screenshots: `_bkk_redirect_test.png`, `_norway_capped.png`, `_norway_with_ele.png`, `_safari_seg2_check.png`, `_safari_seg2_seek04.png`.
- Tracked dirty path: `_Documentation/experiment-2026-sidebyside-small.mp4` (refreshed today).
- Modified inside speculation: `norway-weekend-2013/manifest.js`, `norway-weekend-2013/manifest.json`, `render.html`.
- No `CLAUDE.md` at project root. Five briefings have now flagged this.
