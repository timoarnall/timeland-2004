# 2026-05-23 — 2004 - Timeland

## Yesterday's calls

- **Q1 (scope — A/B/C).** Switcher built and on its branch. All three readings open in one frame now. Decision still yours to make.
- **Q2 (latitude-aware projection).** No movement. Preview tool and renderer flag both still on their branches.
- **Q3 (combine three additive yeses).** No movement. Still one fast-forward.
- **Q4 (scope front door).** Done — B and C are working pages, A is the live root.

## What's happening

The working branch took only the briefing commit. Every piece the scope decision needs now exists: the matrix, the three reading docs, the static mockups, the three realized pages, and the switcher that loads all three in one frame. There is nothing left to build on this question. It is waiting on your read of which front door a stranger should meet first.

## Direction and drift

The piece stays the faithful 2026 port of the 2004 Iceland work, last touched in code on the eleventh. The engine that plays other trips is the drift, and the scope call decides whether it stays a side folder, sits below the archive with a sign, or takes the front. That call is the only open thread with weight. The two technical questions under it are built and parked.

## Open questions

1. **Scope — A archive / B both with signage / C atlas.** Materials complete. The only friction left was mechanical: comparing meant a branch checkout plus a local server. Tonight removes that.
   - `git checkout overnight/2026-05-23-scope-launcher`, then double-click **Compare scope A-B-C.command** in Finder. It starts a server, opens the switcher, shuts down when you close the window. Keys 1/2/3 switch readings.
   - If one wins cleanly, its file becomes `index.html`. A already is.
2. **Latitude-aware projection.** Still one browser-open away on its branch. Default-off flag or drop it.
3. **Land the three additive yeses as one.** Perf-audit close, speculation README, CLAUDE.md draft — still a single fast-forward.

## Speculative work tonight

- `overnight/2026-05-23-scope-launcher` — a double-click `.command` launcher that serves and opens the A/B/C switcher, so the scope comparison is one click, no terminal. Branched off the switcher so both travel together. Reversible: `git branch -D`.

## Still on the table

- `overnight/2026-05-22-scope-decide-switcher` — the A/B/C switcher in one frame.
- `overnight/2026-05-21-reading-c-realized` — reading C as the atlas page.
- `overnight/2026-05-20-reading-b-realized` — reading B as the signed front door.
- `overnight/2026-05-19-scope-decision-package` — four scope docs plus the matrix.
- `overnight/2026-05-18-scope-landing-mockups`, `overnight/2026-05-17-scope-sketches`.
- `overnight/2026-05-16-scope-question`, `overnight/2026-05-16-projection-preview`.
- `overnight/2026-05-15-combine-yeses`, `overnight/2026-05-15-speculation-index`.
- `overnight/2026-05-14-projection-latitude-aware`, `-perf-audit-close`, `-speculation-readme`, `-claude-md`.

## Details

- HEAD on `experiment/2026-update`: `17283ec` before tonight's briefing commit.
- New branch: `overnight/2026-05-23-scope-launcher`, tip `38e9b7b`, pushed to origin. New file `Compare scope A-B-C.command` (~35 lines, zsh, python3 http.server, finds a free port from 8742, opens `_scope_decide.html`). Branched from `overnight/2026-05-22-scope-decide-switcher` so the switcher and its readings ride along.
- Smoke test: server returns 200 for `_scope_decide.html`, `_scope_reading_b_signage.html`, and `index.html`. `zsh -n` syntax check passes.
- 14 overnight branches now. Last engine code edit: `_speculation_other_datasets/render.html`, 2026-05-16. Last preservation code change: `experiment-2026/index.html`, commit `ceb97a1`, 2026-05-11.
- `_Documentation/todo_unfix_bugs.md`: both items remain DONE. No new TODO/`?` markers outside `_archive_2004`.
- Working tree had been left on the switcher branch by a prior run; returned to `experiment/2026-update`.
