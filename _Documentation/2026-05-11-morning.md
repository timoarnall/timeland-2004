# 2026-05-11 — 2004 - Timeland

Working tree dirty (one harness lock file). Research-only mode: no speculative branches tonight.

## Yesterday's calls

Yesterday = 2026-05-08, the first briefing. Three days on, four of five resolved.

- **Q1 (which is the site?) — landed as 1d.** Faithful port at `index.html`; map version at `_archive_2004/index_map.html`. The experiment is now a third surface on its own branch, with no stated relationship to the canonical pair.
- **Q2 (preserve-vs-fix list) — landed.** Now formalised as §VII.7 charm-vs-experience-cost. `todo_unfix_bugs.md` records the reverts.
- **Q3 (archive README) — landed.** `_archive_2004/README.md` exists.
- **Q4 (EXIF time sanity check) — landed.** Resolved as camera-was-UTC; the finding now feeds `build_imagedata.py` in the experiment.
- **Q5 (CLAUDE.md at root) — dormant, 3 days unpicked.** The hand-won knowledge migrated to `2026-archaeology.md`, code annotations, and the session handover. Treat as a deliberate not-picked-up signal: the durable notes found other homes. Either close the question or write the file.

## What's happening

The piece is shipped, annotated, tidied, and embedded in the elasticspace site. Most of the week's work was a closing arc: a top-to-bottom code annotation pass, a performance pass locking render to 30 fps, the archive consolidation, two bug reverts under the new criterion, and a fresh side-by-side recording rig. The newest commits sit on `experiment/2026-update`, where a 1200-photo / 1024-px rebuild of the same code now runs in parallel to the curated 241-photo archaeology.

## Direction and drift

The two-track shape — preservation at root, reinterpretation on a branch — is honest and still legible. Drift sits at the seams. The archaeology code carries two non-faithful constants flagged as rig calibration that have not been removed. The experiment is described as a verbatim copy of the archaeology HTML, with no mechanism to keep the two in sync if the archaeology code changes. The recording rig lives inside `_Documentation/` rather than tooling.

## Open questions

1. **Is the 1200-photo tail a feature or a smear?** The spike was set up to ask this; the side-by-side exists; no call has been made.
   - a. Keep 241. The artists curated; that curation *is* the work. The experiment becomes documentation of a road not taken.
   - b. Switch to 1200 and accept the diaristic reading. *Time that land forgot* becomes a record of what the camera saw, not what the artists chose.
   - c. Keep both as published, surface a small index at root that frames the pair as "two readings of the same data". Closest to current state; commits to neither.
2. **PHOTO_ALPHA_CAP at 5× density.** Constant was tuned for 241 photos. Re-tune at 1200 *before* deciding Q1, or the answer to Q1 is partly a tuning artefact.
3. **Non-faithful constants in the archaeology code.** `SCALE_CAP=3800` and the +15% marker bump are rig scaffolding. Either revert (faithful) or document them as a permanent display calibration in §VII.7.
4. **Three article figure GIFs still un-exported** (`comparison-01-first-attempt`, `-05-mid-iteration`, `-09-synced`). Not blocking publication. Decide whether to ship or drop the citations.
5. **Safari dark-mode sanity check** on the deployed elasticspace preview. Worth doing once the site lands there; nothing to do until then.

## Speculative work tonight

None. Working tree has an uncommitted deletion. Branches not created. Any of the above remains a one-commit move on a clean tree.

## Stale speculative branches

None.

## Details

- Branch: `experiment/2026-update`, 5 commits ahead of `main`: `030659f`, `9694571`, `bcce6aa`, `47500bb`, `cebe841`.
- Dirty file: `.claude/scheduled_tasks.lock` (harness state, deletion only).
- Experiment counts: 1458 candidates → 138 pre-GPX-window drops → 120 md5 dupes → 1200 kept.
- GPX time window: 2004-06-30T13:51:31Z to 2004-07-05T19:40:08Z.
- Experiment artefacts: `experiment-2026-sidebyside.mp4` (47 MB, 1100×580, 205 s); `.gif` (26 MB, 600×316, 3 fps); `experiment-2026/data/photos/` (~183 MB, 1200 files, 1024×683 q=80, gitignored).
- Validation: `comparison_v14_validation.{png,gif,tsv}` (8-frame OCR-anchored grid).
- Non-faithful constants in `index.html`: `SCALE_CAP=3800` (vs SWF v19's 2500), +15% marker bump.
- Reverted bugs: longitude sign in `drawDisplay`; `GMT+0200` → `GMT` in `fmtFlashDate`. Recorded in `_Documentation/todo_unfix_bugs.md`.
- Reference docs: `_Documentation/2026-archaeology.md` (78 KB), `2026-05-10-session-handover.md` (12 KB), `experiment-2026.md` (4.7 KB).
- Live URL: https://timoarnall.github.io/timeland-2004/. Embedded on `elasticspace.pages.dev`.
- Lay essay canonical: `/Volumes/Groke/Projects/2026 - Elasticspace update/07-site/content/timeland-archaeology.json` (~86 blocks).
- No `CLAUDE.md` at `/Volumes/Groke/Projects/2004 - Timeland/CLAUDE.md`.
