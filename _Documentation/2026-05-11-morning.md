# 2026-05-11 — 2004 - Timeland

End-of-day refresh. Original morning briefing is preserved in git at `99c49aa`. Working tree is dirty (the new `_speculation_other_datasets/` folder is untracked), so research-only mode: no speculative branches tonight.

## Yesterday's calls

The five questions from this morning's briefing, graded against the rest of the day:

- **Q1 (1200 vs 241).** Dormant. No call made. The day's energy went sideways into other datasets rather than resolving this one.
- **Q2 (PHOTO_ALPHA_CAP re-tune at 5× density).** Ignored. Constant unchanged.
- **Q3 (non-faithful constants).** Partially addressed. `SCALE_CAP=3800` is now explained in archaeology §X.11. The +15% marker bump is annotated inline but not lifted into the archaeology record alongside it.
- **Q4 (three article figure GIFs).** Ignored. No export.
- **Q5 (Safari dark-mode check).** Dormant, waiting on deployment.

## What's happening

The day split into two arcs. The morning was a focused performance pass on the 1200-photo experiment: a thumbnail tier, an FPS readout, a decode pre-warm, state hoisting, and viewport culling all landed. One fix in that audit — an ImageBitmap cache — crashed the browser at 1200-photo density and was reverted the same day, leaving the browser's own raster cache in charge. The afternoon turned to something else entirely: a new folder ranking and prototyping other photographic trips as candidates for the same renderer, with one of them (Bangkok–Burma 2012) already fully built out to render-ready state.

## Direction and drift

Drift, at the direction level, has arrived. The new folder is not a Timeland update. It is "what if the Timeland physics and visual grammar applies to any trip with photos and GPS?" — a pivot from preserving one piece to generalising the engine into a tool. The tooling is past sketching: a dataset builder, a forked renderer, a candidate-ranking document, and one dataset fully materialised. The Timeland piece itself is shipped, embedded, and quiet. The question is whether to keep this exploration inside `2004 - Timeland/` or treat it as a new project that happens to share an ancestor.

## Open questions

1. **Is `_speculation_other_datasets/` a Timeland sub-project or its own thing?**
   - a. Keep it here as a wing. The renderer is forked from this code; the lineage is honest. Costs: the repo's mission widens from "preserve a 2004 work" to "preserve and extend".
   - b. Spin it out to a sibling project (`2026 - Photo trip atlas` or similar). Cleaner mission boundaries. One-time cost of git-mv and a new repo on day one.
   - c. Park it. Treat the day's exploration as a sketch, commit it under a `_speculation_*/` path with a README that says "not picked up", revisit only if a specific trip earns its own piece.
2. **The 1200-vs-241 question (Q1 from this morning) is now three days dormant.** Either decide, or accept that the experiment branch is itself the answer: a road shown, not taken. Worth saying explicitly in the experiment README rather than leaving it ambiguous.
3. **ImageBitmap revert as a permanent answer.** The OOM at ~3.4 GB resident is a real constraint, not a tuning issue. Document it in `experiment-2026-perf-audit.md` as a closed door so a future session doesn't reopen it.
4. **+15% marker bump.** Either lift it into archaeology §X alongside `SCALE_CAP`, or revert it. Sitting between annotated and explained is the worst state.
5. **CLAUDE.md at root.** Still absent. Now more relevant: with a speculation folder exploring generalisation, a project-root operating note would help future sessions tell the preservation work from the tool-building.

## Speculative work tonight

None. Dirty tree (the speculation folder is untracked). Each of the above is a one-commit move on a clean tree.

## Stale speculative branches

None.

## Details

- Perf commits today (all 2026-05-11): `5bdec68` thumbnail tier + cull-if-tiny; `95bc204` `?fps=1` readout; `ebf70f0` audit pass (decode pre-warm + state hoist + ImageBitmap cache); `632f7c1` revert ImageBitmap cache (OOM); `400942e` viewport cull + remove minutefront cross.
- Perf-audit outcomes: fix A (img.decode pre-warm) landed; fix B (ImageBitmap cache) reverted — 1200 × 1024-wide bitmaps ≈ 3.4 GB resident, crashed renderer; fix C and D (state hoists) landed; viewport cull and minutefront removal landed.
- New untracked folder `_speculation_other_datasets/`:
  - `CANDIDATES.md` — 109 lines, ranks 5 candidate trips: BKK–Burma 2012 strongest; Summer 2010 continental; London weekend Moves-era; daily-life slices 2013–2016; Madrid as a failure case.
  - `build_dataset.py` — 260 lines, EXIF/GPS manifest builder, read-only.
  - `render.html` — 556 lines, standalone renderer fork that reads a manifest JSON.
  - `all_tracks.txt` — 6,890 GPX/TCX/FIT files inventoried across the archive.
  - `density_photoarchive_top40.txt` — modified, now 40 rows of `count date` (top: 1295 on 2017-06-26; 942 on 2014-03-19).
  - `bkk-burma-2012/` — fully materialised: 936 photos thumbnailed at 1024-wide, 2,511 GPX trkpts from a Garmin eTrex (2012-04-18 to 2012-04-27 UTC), `bkk-burma-2012.json` manifest written, 5 checkpoint renders captured at seek 10/30/50/70/90.
- Dirty paths: `_speculation_other_datasets/density_photoarchive_top40.txt` (modified, was empty); `_speculation_other_datasets/{CANDIDATES.md, build_dataset.py, render.html, all_tracks.txt, bkk-burma-2012.*}` (untracked); `_Documentation/experiment-2026-sidebyside-small.mp4` (untracked, new recording).
- HEAD: `400942e`. Branch `experiment/2026-update`, 10 commits ahead of `main`.
- No `CLAUDE.md` at project root.
