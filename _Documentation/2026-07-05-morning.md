# 2026-07-05 — 2004 - Timeland

## Yesterday's calls

- **Place names into the visual language.** Picked up and pushed further. Yesterday's version drew every name from the first frame as a static itinerary. Tonight there's a second reading on its own branch where each name blooms as the trail reaches it, then stays.
- **Q1 (datasets front door).** Still on the table; the menu waits on its branch.
- **Q2 (241 vs 1200 density).** Still on the table; the explorer covers it.
- **Q3 (scope B vs C).** Still yours; the apparatus carries the merge commands.
- **Q4 (latitude projection).** Still on the table: ship default-off or drop.

## What's happening

The core piece is done and live at the root. The working branch is quiet and the fresh material stays in the speculation folder. The place-labels idea now has two shapes to choose between: the names as a fixed map annotation, or the names arriving in time with the journey the way the photos do. Tonight's branch borrows each check-in's clock from the trail point it sits nearest, so a name eases in exactly when you pass it and the weekend fills in name by name.

## Direction and drift

The renderer keeps growing into a portable tool for trips other than Iceland, and the place-labels layer is the newest vocabulary in that thread. It stays cordoned to Moves-era data, so Iceland is untouched by construction. The drift worth naming is scope, not code: the speculation folder now carries a forked renderer, a build pipeline, and its own annotation layer, all sitting a fast-forward away from the core rather than merged into it.

## Open questions

1. **Place names: static itinerary or timed bloom?**
   - Static: the whole map reads at once, an overview you can plan against. `git checkout overnight/2026-07-04-place-labels`.
   - Timed: names arrive with the clock, closer to a diary than a map. `git checkout overnight/2026-07-05-place-labels-timed`, serve the folder, open `render.html?dataset=norway-weekend-2013`, press play.
2. **Datasets front door: menu or private lab?** The menu is built; presentation call, not tooling.
3. **241 curated or 1200 as-shot?** The explorer's sliders answer faster than argument.
4. **Scope B vs C.** The execution sheet has the commands for whichever reading you pick.
5. **Latitude projection.** Ship default-off or drop; the preview covers Bangkok and Norway.

## Speculative work tonight

- `overnight/2026-07-05-place-labels-timed` — place labels that bloom with the clock. Each Foursquare check-in takes the timestamp of the nearest trail point, so its dot pulses and its name eases in as the trail arrives, then holds. Seeking snaps labels to their settled state; scrubbing back hides the ones not yet reached. Verified with headless captures at four seek points. Reversible: `git branch -D` if the static version reads better.

## Still on the table

- `overnight/2026-07-04-place-labels` — the static-itinerary version of the same layer.
- `overnight/2026-07-03-datasets-index` — the trips menu with strongest-segment deep-links.
- `overnight/2026-05-27-density-explorer` — photos and linger sliders over the experiment.
- The scope apparatus: execution sheet, triptych, launcher, switcher, readings B and C as pages.
- The projection pair: latitude-aware implementation and on/off preview.
- `overnight/2026-05-15-combine-yeses` — perf-close, speculation README, CLAUDE.md draft in one fast-forward.

## Details

- HEAD on `experiment/2026-update`: `eec4556`.
- New branch, pushed: `overnight/2026-07-05-place-labels-timed`, off `overnight/2026-07-04-place-labels`.
- Change is `_speculation_other_datasets/render.html` only: each place gets `t` from the nearest trail point in map space, then a `timer` ramps 0→100 at `FADE_PER_SWF_FRAME * 0.6` (slower than photos) once the clock passes it.
- Dot swells from 4.5 px to 2 px as the label ramps in; label and dot alpha scale with the reveal.
- Verification captures: `_screenshots/norway_timed_seek{010,040,070,100}.png`. seek 0.10 shows no labels yet, 0.70 shows the Dovrefjell names bloomed, 1.0 shows the full route home to Oslo.
- Known limit carried over: at full zoom-out the mountain cluster's labels collide and suppress; individual names read once zoomed in.
- 23 `overnight/*` branches total, all on origin.
