# 2026-05-25 — 2004 - Timeland

## Yesterday's calls

- **Q1 (scope — A/B/C).** Still yours. No new scaffolding; the call is a taste call, not a making one.
- **Q2 (latitude-aware projection).** No movement. Preview tool and flag still on their branches.
- **Q3 (combine three additive yeses).** No movement. Still one fast-forward.
- **Q4 (scope front door).** Settled earlier — A is the live root, B and C are working pages.

## What's happening

The working branch took only briefing commits this week. Every piece of the scope decision is built and parked: the matrix, the reading docs, three realized pages, the one-frame switcher, the launcher. The two technical questions, latitude-aware projection and the three additive yeses, are also built and parked on their branches. Nothing mechanical sits between you and any of these calls.

## Direction and drift

The piece stays the faithful 2026 port of the 2004 Iceland work, last touched in code on the eleventh. The drift is still the engine that plays other trips in `_speculation_other_datasets`. The scope call decides whether that stays a side folder, sits below the archive with a sign, or takes the front. That one call carries all the weight here.

## Open questions

1. **Scope — A archive / B both with signage / C atlas.** The real fork is B versus C: keep the archive primary with a signed engine wing below, or put the engine first with Iceland as one trip of four. A means do nothing, and that is a real answer.
   - Glance: `_Documentation/scope-triptych-A-B-C.png` shows all three side by side in the morning page, no checkout needed.
   - Feel: `git checkout overnight/2026-05-22-scope-decide-switcher`, open `_scope_decide.html`, press 1 / 2 / 3.
2. **Latitude-aware projection.** One browser-open away on its branch. Bangkok is the dramatic case, Norway the sanity check. The main Iceland piece renders unchanged by construction, so this is opt-in for the other-trips renderer. Ship default-off, or drop it.
3. **Land the three additive yeses as one.** Perf-audit close, speculation README, CLAUDE.md draft — a single fast-forward onto the working branch when you want them.

## Speculative work tonight

- `overnight/2026-05-25-scope-triptych` — the three front doors captured as one still, `_Documentation/scope-triptych-A-B-C.png`. A reads pale because it is the live piece mid-play; B and C read fully, which is where the decision lives. Reversible: `git branch -D` if not useful.

## Still on the table

- `overnight/2026-05-23-scope-launcher` — one-click `.command` launcher for the switcher.
- `overnight/2026-05-22-scope-decide-switcher` — the A/B/C switcher in one frame.
- `overnight/2026-05-21-reading-c-realized`, `overnight/2026-05-20-reading-b-realized` — readings C and B as pages.
- `overnight/2026-05-19-scope-decision-package` — four scope docs plus the matrix.
- `overnight/2026-05-18-scope-landing-mockups`, `-2026-05-17-scope-sketches`, `-2026-05-16-scope-question`.
- `overnight/2026-05-16-projection-preview`, `overnight/2026-05-14-projection-latitude-aware`.
- `overnight/2026-05-15-combine-yeses`, `overnight/2026-05-15-speculation-index`.
- `overnight/2026-05-14-perf-audit-close`, `-speculation-readme`, `-claude-md`.

## Details

- HEAD on `experiment/2026-update`: `b44e16a` before tonight's briefing commit.
- New branch: `overnight/2026-05-25-scope-triptych`, built from `754933f` (switcher). Adds `_scope_triptych.html`, `_Documentation/scope-triptych-A-B-C.png` (2100×820, 122 KB), `_Documentation/scope-triptych-note.md`.
- 15 overnight branches before tonight, all pushed to origin. Scope chain tips: `38e9b7b` (launcher) → `754933f` (switcher) → `fd119b8` (reading-c) → `830b1e6` (reading-b) → `7f653dd` (decision-package).
- Latitude flag: `render.html?latfit=1` rescales longitude by `cos(centerLat)/cos(64.5°)`; Iceland reference latitude leaves the main piece unchanged.
- Last engine code edit: `_speculation_other_datasets/render.html`, 2026-05-16. Last preservation code change: `experiment-2026/index.html`, `ceb97a1`, 2026-05-11.
- `_Documentation/todo_unfix_bugs.md`: both bug-revert items remain DONE. No new TODO / `?` markers outside `_archive_2004`.
- Experiment corpus: 1200 photos, 1024px source, ~183 MB. `PHOTO_ALPHA_CAP = 0.2`.
