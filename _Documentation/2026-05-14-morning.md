# 2026-05-14 — 2004 - Timeland

## Yesterday's calls

- **Q1 (speculation folder: commit / move / quarantine).** Partly resolved. The folder's contents went from untracked to committed in place. The "what is this folder" declaration didn't get written — addressed tonight on a branch.
- **Q2 (occlusion cull as the closing perf chapter).** Available. Perf-audit doc unchanged on main — addressed tonight on a branch.
- **Q3 (the five-question pattern: signal or noise).** Available. No explicit call recorded.
- **Five older items.** Still on the table.

## What's happening

Two threads moved today. The first is a marker-scale cap inside the speculation renderer that stops high-altitude datasets blowing their hour-cross sizes off the canvas. The second is a redirect stub for the combined BKK-Burma dataset and a Norway elevation backfill that fills the zeros Moves doesn't record. Both went onto the working branch as part of a save-in-progress commit, so what was three days of growing-but-untracked side-folder work is now in the repo.

## Direction and drift

The preservation mission is shipped and quiet. The experiment is perf-closed. The drift, if it is drift, is the side-folder — three datasets, a forked renderer, a builder, an elevation helper, a findings doc. The repo is now visibly carrying two missions in parallel, and the side-folder is no longer hiding. That makes the "where does this live" call easier than it was yesterday, not harder.

## Open questions

1. **The side-folder is committed. Where does it live long-term?**
   - a. Stay in this repo. Honest lineage. A README declares scope.
   - b. `git mv` to its own repo. Cleaner boundary, different mission.
   - c. Park as `_attic/`. Commits the work to history without endorsing the pivot.
   - Branch tonight writes (a)'s README — `git checkout overnight/2026-05-14-speculation-readme` to read.
2. **Latitude-aware projection for non-Iceland trips.**
   - a. Keep Iceland's hand-tuned 1:2 lon:lat. Bangkok rendered as if it were Iceland. Defensible as style.
   - b. Rescale lon by `cos(centerLat) / cos(64.5°)`. Iceland unchanged, Bangkok widens to its real proportions, Summer 2010 stretches ~1.5×.
   - Branch tonight implements (b) behind `?latfit=1` — `git checkout overnight/2026-05-14-projection-latitude-aware` to compare. Note alongside.
3. **Close the perf chapter in writing.**
   - a. Append the occlusion-cull paragraph to `experiment-2026-perf-audit.md`. Done on a branch — `git checkout overnight/2026-05-14-perf-audit-close` to read.
   - b. Leave it. Story ends on viewport cull, the cluster cull lives only in commit history.
4. **Project CLAUDE.md.** Drafted on a branch with the criterion, the sensitive paths, the two-mission split, the voice rules. `git checkout overnight/2026-05-14-claude-md` to read. Keep, edit, or drop.

## Speculative work tonight

- `overnight/2026-05-14-perf-audit-close` — appends the occlusion-cull closing section to the perf-audit doc. Reversible: `git branch -D` if not useful.
- `overnight/2026-05-14-speculation-readme` — writes `_speculation_other_datasets/README.md` declaring scope and listing three reasonable next moves. Reversible: `git branch -D`.
- `overnight/2026-05-14-projection-latitude-aware` — adds `?latfit=1` to the speculation renderer, computes the mean latitude, rescales longitude by `cos(centerLat) / cos(64.5°)`. Includes a comparison note. Reversible: `git branch -D`.
- `overnight/2026-05-14-claude-md` — drafts a root `CLAUDE.md`. Reversible: `git branch -D`.

## Still on the table

No `overnight/*` branches survive from prior nights — they were research-only briefings.

## Details

- HEAD on `experiment/2026-update`: `27b5205` "Save work in progress 2026-05-14". 20 commits ahead of `main`.
- Today's commit `27b5205` covers: `MARKER_SCALE_CAP = 1.6` inside `_speculation_other_datasets/render.html`; `lookup_elevation.py` (91 lines, hits `ws.geonorge.no/hoydedata/v1/punkt`); redirect stubs in `bkk-burma-2012/manifest.js` and `summer-2010/manifest.js` (7 lines each, `location.replace` to `__seg2`); Norway elevation values backfilled from zero; refreshed `_Documentation/experiment-2026-sidebyside-small.mp4` (~15.7 MB); five new screenshots under `_speculation_other_datasets/_screenshots/`.
- Branches pushed tonight: `overnight/2026-05-14-perf-audit-close`, `overnight/2026-05-14-speculation-readme`, `overnight/2026-05-14-projection-latitude-aware`, `overnight/2026-05-14-claude-md`. All on `origin`.
- `_Documentation/todo_unfix_bugs.md` items both marked DONE (longitude formula, GMT clock).
- No new `TODO` / `FIXME` markers in changed files.
- Speculation folder now: `bkk-burma-2012` + segments 1–4, `summer-2010` + segments 1–6, `norway-weekend-2013`, plus `CANDIDATES.md`, `FINDINGS.md`, `build_dataset.py`, `lookup_elevation.py`, `render.html`.
- Latitude-fit multipliers for the three datasets: Bangkok ~13.7°N → 2.27×, Summer 2010 mean ~49°N → 1.51×, Norway ~62°N → 1.09×.
