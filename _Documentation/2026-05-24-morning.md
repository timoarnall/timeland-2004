# 2026-05-24 — 2004 - Timeland

## Yesterday's calls

- **Q1 (scope — A/B/C).** Launcher built on its branch. Double-click serves and opens the switcher, no terminal. Decision still yours.
- **Q2 (latitude-aware projection).** No movement. Preview tool and renderer flag both still on their branches.
- **Q3 (combine three additive yeses).** No movement. Still one fast-forward.
- **Q4 (scope front door).** Done — A is the live root, B and C are working pages.

## What's happening

The working branch took only the briefing commit again. Every part of the scope decision is built: the matrix, the reading docs, the three realized pages, the one-frame switcher, and a one-click launcher. There is nothing mechanical left between you and the call. I'm not building a fifteenth scope artifact tonight, because more scaffolding around a finished question is noise, not help. The question is settled as a making problem and open only as a taste call.

## Direction and drift

The piece stays the faithful 2026 port of the 2004 Iceland work, last touched in code on the eleventh. The engine that plays other trips, in `_speculation_other_datasets`, is still the drift, and the scope call is what decides whether it stays a side folder, sits below the archive with a sign, or takes the front. That call is the only open thread with weight.

## Open questions

1. **Scope — A archive / B both with signage / C atlas.** Materials complete. Fastest path to decide:
   - `git checkout overnight/2026-05-23-scope-launcher`, double-click **Compare scope A-B-C.command** in Finder, press 1/2/3 to switch front doors. Pick the one a stranger should meet first.
   - When one wins, its file becomes `index.html`. A already is, so "do nothing" is a real answer.
2. **Latitude-aware projection.** Still one browser-open away on its branch. Default-off flag or drop it.
3. **Land the three additive yeses as one.** Perf-audit close, speculation README, CLAUDE.md draft — still a single fast-forward onto the working branch when you want them.

## Speculative work tonight

None. The scope question's materials are exhausted; the two technical questions are built and parked. Adding another branch would pad the count without giving you anything new to poke at. The honest move is to stop building around this decision and let you make it.

## Still on the table

- `overnight/2026-05-23-scope-launcher` — one-click `.command` launcher for the A/B/C switcher.
- `overnight/2026-05-22-scope-decide-switcher` — the A/B/C switcher in one frame.
- `overnight/2026-05-21-reading-c-realized` — reading C as the atlas page.
- `overnight/2026-05-20-reading-b-realized` — reading B as the signed front door.
- `overnight/2026-05-19-scope-decision-package` — four scope docs plus the matrix.
- `overnight/2026-05-18-scope-landing-mockups`, `overnight/2026-05-17-scope-sketches`, `overnight/2026-05-16-scope-question`.
- `overnight/2026-05-16-projection-preview`, `overnight/2026-05-14-projection-latitude-aware`.
- `overnight/2026-05-15-combine-yeses`, `overnight/2026-05-15-speculation-index`.
- `overnight/2026-05-14-perf-audit-close`, `-speculation-readme`, `-claude-md`.

## Details

- HEAD on `experiment/2026-update`: `049afd9` before tonight's briefing commit.
- 14 overnight branches, all pushed to origin. Scope chain tips: `38e9b7b` (launcher, 05-23) → `754933f` (switcher, 05-22) → `fd119b8` (reading-c, 05-21) → `830b1e6` (reading-b, 05-20) → `7f653dd` (decision-package, 05-19).
- Last engine code edit: `_speculation_other_datasets/render.html`, 2026-05-16. Last preservation code change: `experiment-2026/index.html`, commit `ceb97a1`, 2026-05-11.
- `_Documentation/todo_unfix_bugs.md`: both bug-revert items remain DONE. No new TODO / `?` markers outside `_archive_2004`.
- Experiment corpus: 1200 photos, 1024px source, ~183 MB. `PHOTO_ALPHA_CAP = 0.2`.
- No 05-21 morning file exists; this is the first 05-24 file.
