# 2026-05-27 — 2004 - Timeland

## Yesterday's calls

- **Q1 (scope — A/B/C).** Still yours. Apparatus complete; the execution sheet from the 26th is on its branch. Nothing new built tonight to push it further — it's a taste call, not a tooling gap.
- **Q2 (latitude-aware projection).** No movement. Preview with on/off screenshots stays on its branch.
- **Q3 (combine three additive yeses).** No movement. Still one fast-forward when you want it.
- **Q4 (scope front door).** Stays settled — A is the live root, B and C are working pages.

## What's happening

The working branch took only briefing commits this week. Every mechanical thing the scope call needs has existed for days, so I stopped adding to that pile. Instead I went at the one open thread that has never had an artefact: whether the trip reads better at 241 curated photos or the full 1200 the camera shot. That question has surfaced in the docs since the experiment started but only ever as prose. Tonight it gets two sliders.

## Direction and drift

The piece stays the faithful 2026 port of the 2004 Iceland work, last touched in code on the eleventh. The scope fork (side folder vs below-the-archive vs front-page atlas) is the only call with weight and it's waiting on you, not on more pages. The density question is a quieter thread but a real one, and it sits inside the preservation piece rather than the other-trips engine, so chasing it doesn't widen scope.

## Open questions

1. **241 curated or 1200 as-shot?** Honesty or noise.
   - Feel it: `git checkout overnight/2026-05-27-density-explorer`, serve `experiment-2026/`, open `density-explorer.html`. Drag the photos slider down toward ~241 and watch the comet tails thin; drag linger to see how long each photo holds after its moment.
   - Or leave it: 1200 is the current experiment default and reads as diaristic.
2. **Scope — A archive / B both with signage / C atlas.** The fork is B versus C; A means keep the live root and is a real answer. Glance at `_Documentation/scope-triptych-A-B-C.png`; the execution sheet has the merge commands for whichever you pick.
3. **Latitude-aware projection.** Ship default-off or drop. Preview covers Bangkok and Norway with on/off; Iceland renders unchanged by construction.
4. **Land the three additive yeses as one.** Perf-audit close, speculation README, CLAUDE.md draft — a single merge.

## Speculative work tonight

- `overnight/2026-05-27-density-explorer` — live density explorer over the experiment engine: photos slider (subsample 120–1200, evenly spread) and linger slider (live alpha cap). Verified it serves and both data files load; I did not drive the canvas in a browser, so the slider feel is untested. Reversible: `git branch -D` if not useful.

## Still on the table

- `overnight/2026-05-26-decision-execution-sheet` — merge-and-cleanup commands for each scope call.
- `overnight/2026-05-25-scope-triptych` — three front doors as one still.
- `overnight/2026-05-23-scope-launcher`, `overnight/2026-05-22-scope-decide-switcher` — launcher and one-frame A/B/C switcher.
- `overnight/2026-05-21-reading-c-realized`, `overnight/2026-05-20-reading-b-realized` — readings C and B as pages.
- `overnight/2026-05-19-scope-decision-package`, `-2026-05-18-scope-landing-mockups`, `-2026-05-17-scope-sketches`, `-2026-05-16-scope-question`.
- `overnight/2026-05-16-projection-preview`, `overnight/2026-05-14-projection-latitude-aware`.
- `overnight/2026-05-15-combine-yeses`, `-speculation-index`.
- `overnight/2026-05-14-perf-audit-close`, `-speculation-readme`, `-claude-md`.

## Details

- HEAD on `experiment/2026-update`: `b2832a5` before tonight's briefing commit.
- New branch: `overnight/2026-05-27-density-explorer`, from `experiment/2026-update`. Adds `experiment-2026/density-explorer.html` (copy of `experiment-2026/index.html`, 1306 lines, + control panel + 3 wiring edits).
- Subsample uses a stable rank `(i × 0.61803398875) mod 1`; `keep` slider 0.1–1.0, linger slider 0.05–1.0.
- Experiment corpus: 1200 photos, 213 MB in `experiment-2026/data` (320px + 1024px tiers). `PHOTO_ALPHA_CAP` default 1.0 in the experiment, 0.2 in the archaeology.
- Archaeology curated count: 241 photos.
- Served locally on :8731 for the check; page 200 (62,603 b), gpx 200, xml 200.
- 18 overnight branches total, all pushed to origin.
