# 2026-05-15 — 2004 - Timeland

## Yesterday's calls

- **Q1 (where the speculation folder lives).** Available. README branch from yesterday still alongside, no merge.
- **Q2 (latitude-aware projection).** Available. `?latfit=1` branch from yesterday still alongside.
- **Q3 (close the perf chapter in writing).** Available. Closing-paragraph branch from yesterday still alongside.
- **Q4 (project CLAUDE.md).** Available. Draft branch from yesterday still alongside.

## What's happening

Quiet on the working branch today. The four overnight branches from yesterday — perf-audit close, speculation README, latitude-aware projection, CLAUDE.md draft — are all alongside `experiment/2026-update`. None merged, none diverged further. Three of those four are pure additions (one new file or one appended section each), so they can land as a single fast-forward if the answers are yes-yes-yes.

## Direction and drift

The preservation surface is unchanged from last week: shipped, embedded, archived. The speculation surface keeps building under it — three datasets with eleven playable segments, a forked renderer, a generic builder, an elevation helper, screenshots — but it has no front door inside the folder yet. From the file tree it reads like research-in-progress rather than something you can navigate.

## Open questions

1. **Land the three additive yeses as one.** The perf-audit close, the speculation README, and the project CLAUDE.md are independent of each other and each touches one file. They were drafted as separate branches yesterday so each could be vetoed independently. If all three read OK, a single merge ends three open questions at once.
   - a. Fast-forward `overnight/2026-05-15-combine-yeses` into `experiment/2026-update`. Three files added, nothing else changes.
   - b. Land them individually as before. Keeps the audit trail granular.
2. **Speculation folder needs a front door.** Three datasets are clickable but invisible — nothing at `_speculation_other_datasets/` lists them.
   - a. Land `overnight/2026-05-15-speculation-index` — a single `index.html` with one row per trip, a screenshot, and segment links. Pure addition.
   - b. Leave the folder flat; the JSON files document themselves.
3. **Latitude-aware projection (carried from yesterday).** `?latfit=1` branch still sits. The deciding question is whether you want to see Bangkok with its real proportions before answering.
   - a. Default off, flag on. `git checkout overnight/2026-05-14-projection-latitude-aware` and visit `render.html?dataset=bkk-burma-2012__seg2&latfit=1`.
   - b. Default on. The Iceland piece is unchanged either way (`cos(64.5°)/cos(64.5°) = 1`), so this is safe.
   - c. Drop the flag. Style call: every trip rendered in the Iceland projection is the through-line.
4. **Where the speculation folder lives long-term (carried).** Yesterday's three variants stand. No branches needed before answering — it's a where-not-what question.

## Speculative work tonight

- `overnight/2026-05-15-combine-yeses` — three additive commits (perf-audit close, speculation README, CLAUDE.md draft) cherry-picked onto today's HEAD. If you say yes to all three, a single fast-forward lands them. Reversible: `git branch -D`.
- `overnight/2026-05-15-speculation-index` — one new file at `_speculation_other_datasets/index.html`. Lists the three trips with a preview screenshot and segment links. `git checkout` to read. Reversible: `git branch -D`.

## Still on the table

- `overnight/2026-05-14-perf-audit-close` — appends the cluster-cull paragraph to the perf-audit doc.
- `overnight/2026-05-14-speculation-readme` — declares scope for the speculation folder.
- `overnight/2026-05-14-projection-latitude-aware` — adds `?latfit=1` to the renderer.
- `overnight/2026-05-14-claude-md` — drafts a root project CLAUDE.md.

## Details

- HEAD on `experiment/2026-update`: `5e2a535` "Overnight briefing 2026-05-14". 20 commits ahead of `main`. No commits today on the working branch.
- `overnight/2026-05-15-combine-yeses` (HEAD `787d95f`): cherry-picks `ebe2ec3` (`_Documentation/experiment-2026-perf-audit.md` +24), `a86821a` (`_speculation_other_datasets/README.md` +32), `35245e8` (`CLAUDE.md` +53). Three commits, three files, no overlap.
- `overnight/2026-05-15-speculation-index` (one commit): `_speculation_other_datasets/index.html` +106 lines. References three existing screenshots (`bkk_seek_030.png`, `summer_seek_050.png`, `norway_seek_055.png`) and the existing dataset slugs.
- Datasets enumerated in the index, with photo and trkpt counts: bkk-burma-2012 seg1–4 (122/290/233/140 photos, 639/735/650/241 trkpts); summer-2010 seg1–6 (548/14/250/2/8/25 photos, 1084/323/1768/218/930/551 trkpts); norway-weekend-2013 (449 photos, 1900 trkpts).
- Latfit multipliers if `?latfit=1` lands: Bangkok 2.27×, Summer 2010 1.51×, Norway 1.09×; Iceland 1.00× by construction.
- Branches pushed tonight: `overnight/2026-05-15-combine-yeses`, `overnight/2026-05-15-speculation-index`. Both on `origin`.
- `_Documentation/todo_unfix_bugs.md`: both items remain DONE; no new entries.
- No new `TODO` / `FIXME` markers in the repo today.
