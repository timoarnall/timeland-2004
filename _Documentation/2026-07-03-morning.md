# 2026-07-03 — 2004 - Timeland

## Earlier calls

- **Q1 (scope — A/B/C).** Still yours. All the apparatus exists (execution sheet, triptych still, launcher, switcher). It's a taste call, not a tooling gap.
- **Q2 (latitude-aware projection).** On its branch with on/off preview. Available to ship default-off or drop.
- **Q3 (combine three additive yeses).** Still one fast-forward when you want it: perf-audit close, speculation README, CLAUDE.md draft.
- **Q4 (scope front door).** Settled — A is the live root, B and C are working pages.

## What's happening

The perf audit closed with the occlusion-cull chapter, so the whole viewport and culling cycle is written down and done. The other-datasets folder got a full handover doc and a refreshed findings write-up: how the renderer transplants to Bangkok-Burma, the 2010 summer trip, and the Oppdal weekend, plus the flight-splitter, elevation-lookup, and marker-cap decisions that made each one read right. That folder is where the fresh energy is.

## Direction and drift

The piece is still the faithful 2026 port of the 2004 Iceland work, live at the root and accurate to the original film. Around it sit two threads that are ready whenever you want them: the other-datasets engine (the renderer as a portable visualisation tool) and the 241-vs-1200 density question inside the experiment. Neither widens the core preservation; both are optional readings waiting on your taste, not on more building.

## Open questions

1. **The other-datasets engine needs a front door.** Right now opening a trip means knowing the exact segment slug.
   - Variant a: a menu page listing all three trips, strongest segment marked, deep-links to the canonical moment. Built tonight — `git checkout overnight/2026-07-03-datasets-index`, serve the folder, open `index.html`.
   - Variant b: leave it slug-only and treat the folder as a private lab, not something to present.
2. **241 curated or 1200 as-shot?** Honesty or noise.
   - Feel it: `git checkout overnight/2026-05-27-density-explorer`, serve `experiment-2026/`, open `density-explorer.html`, drag the photos slider.
   - Or leave 1200 as the experiment default, which reads as diaristic.
3. **Scope — A archive / B both with signage / C atlas.** The fork is B versus C; A is a real answer. `_Documentation/scope-triptych-A-B-C.png` and the execution sheet carry the merge commands for whichever you pick.
4. **Latitude-aware projection.** Ship default-off or drop. Preview covers Bangkok and Norway; Iceland renders unchanged by construction.

## Speculative work tonight

- `overnight/2026-07-03-datasets-index` — a menu for the other-datasets folder. Three trips, strongest segment flagged per trip, deep-links to the canonical seek (Yangon at 15%, the continental-scale summer leg, the Oppdal corner case), and a link back to the Iceland original so the "same renderer, different data" point is legible. Uses the existing preview screenshots. Verified it serves and all links and thumbnails resolve; I did not click through into each render in a browser. Reversible: `git branch -D` if not useful.

## Still on the table

- `overnight/2026-05-15-speculation-index` — the earlier menu attempt; tonight's index is a fresh take off current HEAD, so this one is superseded but there to compare.
- `overnight/2026-05-27-density-explorer` — photos and linger sliders over the experiment.
- `overnight/2026-05-26-decision-execution-sheet`, `-2026-05-25-scope-triptych`, `-2026-05-23-scope-launcher`, `-2026-05-22-scope-decide-switcher` — the scope decision apparatus.
- `overnight/2026-05-21-reading-c-realized`, `-2026-05-20-reading-b-realized` — readings C and B as pages.
- `overnight/2026-05-19-scope-decision-package`, `-2026-05-18-scope-landing-mockups`, `-2026-05-17-scope-sketches`, `-2026-05-16-scope-question`.
- `overnight/2026-05-16-projection-preview`, `-2026-05-14-projection-latitude-aware`.
- `overnight/2026-05-15-combine-yeses`, `-2026-05-14-perf-audit-close`, `-speculation-readme`, `-claude-md`.

## Details

- HEAD on `experiment/2026-update`: `756eaf8`.
- Recent commits: `ebfd7ea` perf-audit occlusion-cull close, `bb2d0bd` speculation handover + FINDINGS refresh (both 2026-07-02).
- New branch: `overnight/2026-07-03-datasets-index`, from `experiment/2026-update`. Adds `_speculation_other_datasets/index.html` (137 lines). Pushed to origin.
- Perf sweep: 26 seek points, 29.06–30.00 fps; occlusion-stack cull in `540e341`.
- Datasets on disk: 11 segments (`bkk-burma-2012__seg{1..4}`, `summer-2010__seg{1..6}`, `norway-weekend-2013`), ~270 MB. Strongest per trip: seg2 Yangon (290 photos), seg1 Norway (548 photos), Oppdal (449 photos).
- Manifest loading is via `<script>` tag, not `fetch()` — Safari and Chrome both block `fetch()` on `file://`.
- Served locally on :8742 for the check; index.html 200 (7,271 b), render.html 200, preview 200.
- 19 overnight branches total, all pushed to origin.
