# 2026-05-17 — 2004 - Timeland

## Yesterday's calls

- **Q1 (scope question).** Available. `overnight/2026-05-16-scope-question` carries the doc. Tonight's branch builds directly on it.
- **Q2 (latitude-aware projection).** Available. Preview tool unread; renderer flag still on its own branch.
- **Q3 (combine three yeses into one fast-forward).** Available. Still one fast-forward.
- **Q4 (speculation folder front door).** Available. Index page sits alongside.

## What's happening

Working branch quiet — the past week is three nightly briefings and one save commit. All making continues on overnight branches. The engine surface keeps iterating in the working tree; the preservation surface has not moved since the perf-cull a week ago.

## Direction and drift

The drift is the same one named last night, with one new fact: the scope question doc names three readings in prose, but the readings are still abstract. Tonight makes them concrete — for each reading, a file tree, a README opening, and the exact set of `git mv` calls. The aim is to make A, B, and C pickable rather than describable.

## Open questions

1. **Scope — A archive, B both with signage, C atlas.** The companion sketches now show each reading at the level of folder layout and file moves. Reading them side by side may be quicker than the prose alone.
   - Read `_Documentation/scope-sketches.md` on the new branch alongside `experiment-scope-question.md` on yesterday's. Decide; the decision unblocks several other branches.
2. **Latitude-aware projection.** No new movement. The preview tool is one open in the browser.
   - a. Open the tool, decide if the on-shape is the truthful one for non-Iceland data, then land the projection branch (default off, flag on) or its default-on cousin.
   - b. Drop the flag — Iceland projection as through-line.
3. **Land the three additive yeses as one (carried).** Perf-audit close, speculation README, CLAUDE.md draft, still a single fast-forward.
4. **Speculation folder front door (carried).** One-page index. Downstream of question 1: if reading C wins, this becomes the atlas landing page; if reading B wins, this becomes the engine's index.

## Speculative work tonight

- `overnight/2026-05-17-scope-sketches` — one new doc at `_Documentation/scope-sketches.md`. For each of the three readings: a tree fragment, a README opening paragraph, the literal `git mv` list, and a one-line lost/gained note. Pure addition. Reversible: `git branch -D`.

## Still on the table

- `overnight/2026-05-14-perf-audit-close` — cluster-cull paragraph appended to the perf-audit doc.
- `overnight/2026-05-14-speculation-readme` — declares scope for the speculation folder.
- `overnight/2026-05-14-projection-latitude-aware` — adds `?latfit=1` to the renderer.
- `overnight/2026-05-14-claude-md` — drafts a root project CLAUDE.md.
- `overnight/2026-05-15-combine-yeses` — the three above stacked for a single fast-forward.
- `overnight/2026-05-15-speculation-index` — `_speculation_other_datasets/index.html` listing the three trips.
- `overnight/2026-05-16-scope-question` — names the preservation-vs-engine question with three readings.
- `overnight/2026-05-16-projection-preview` — `_projection_preview.html` plotting all five segments with latfit toggle.

## Details

- HEAD on `experiment/2026-update`: `ef4d753` "Overnight briefing 2026-05-16". Pushed to origin.
- New branch tonight: `overnight/2026-05-17-scope-sketches` — `_Documentation/scope-sketches.md`, ~155 lines, one file. Pushed to origin.
- Nine overnight branches total now: four from 05-14, two from 05-15, two from 05-16, one from 05-17.
- Companion to read alongside tonight's sketch: `overnight/2026-05-16-scope-question` → `_Documentation/experiment-scope-question.md` (~75 lines).
- Files referenced in the sketches that exist today at the working-tree root: `index.html`, `data/`, `experiment-2026/`, `_archive_2004/`, `_Documentation/`, `_speculation_other_datasets/`, `README.md`.
- Files referenced inside `_speculation_other_datasets/`: `render.html`, `build_dataset.py`, `lookup_elevation.py`, `FINDINGS.md`, `CANDIDATES.md`, three dataset folders (`bkk-burma-2012`, `summer-2010`, `norway-weekend-2013`) plus eleven `__seg*` siblings.
- Last edit to working-tree engine code: `_speculation_other_datasets/render.html`, 2026-05-16 03:01 (21,835 bytes).
- Last edit to preservation surface code: `experiment-2026/index.html`, commit `ceb97a1`, 2026-05-11.
- No new `TODO` / `FIXME` markers anywhere outside `_archive_2004`.
- `_Documentation/todo_unfix_bugs.md`: both items remain DONE.
