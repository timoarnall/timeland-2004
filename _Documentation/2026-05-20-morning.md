# 2026-05-20 — 2004 - Timeland

## Yesterday's calls

- **Q1 (scope decision package).** Available. The four docs and the matrix sit on `overnight/2026-05-19-scope-decision-package`, ready to read on one checkout. Not opened yet.
- **Q2 (latitude-aware projection).** Available. Renderer flag and preview tool both on their branches, unread.
- **Q3 (combine three yeses).** Available. Still one fast-forward.
- **Q4 (speculation front door).** Folded into tonight's work — see below.

## What's happening

The working branch took only nightly briefing commits this week. The engine wing and the preservation surface are both untouched. The scope question now has everything it needs to be decided by eye: a matrix, file-move sketches, three landing mockups, four companion docs. What it didn't have was anything to click.

## Direction and drift

The project is the faithful 2026 port of the 2004 Iceland piece, plus an engine that grew on the side and now plays three other trips out of `_speculation_other_datasets/`. The drift is well named after four nights of docs. Tonight stops adding to the paper pile. Instead it builds one of the three readings as a working front door, so the call can be made by using it.

## Open questions

1. **Scope — A archive / B both with signage / C atlas.** The decision materials are complete. Tonight adds the missing thing: reading B as a real, clickable page rather than a mockup.
   - `git checkout overnight/2026-05-20-reading-b-realized` and open `_scope_reading_b_signage.html`. Click into the piece, click into the trips. If it reads right, it becomes `index.html`. If it dilutes the archive, `git branch -D`.
   - Reading A is the current root `index.html` already (the piece alone, no wing) — nothing to build.
   - Reading C (engine first, Iceland one trip of four) is the inversion of tonight's page; buildable next if B reads wrong.
2. **Latitude-aware projection.** No new movement. The preview tool is one browser-open away on its branch.
3. **Land the three additive yeses as one (carried).** Perf-audit close, speculation README, CLAUDE.md draft — still a single fast-forward.

## Speculative work tonight

- `overnight/2026-05-20-reading-b-realized` — `_scope_reading_b_signage.html`, a working front door for reading B. Archive primary at top with the live `index.html` behind it; the engine wing below with real links to the three trips. Reversible: `git branch -D`.

## Still on the table

- `overnight/2026-05-19-scope-decision-package` — four scope docs plus the matrix on one checkout.
- `overnight/2026-05-18-scope-landing-mockups` — three static landing mockups, one per reading.
- `overnight/2026-05-17-scope-sketches` — trees and `git mv` lists for A, B, C.
- `overnight/2026-05-16-scope-question` — names the three readings.
- `overnight/2026-05-16-projection-preview` — `_projection_preview.html`, all five segments with the latfit toggle.
- `overnight/2026-05-15-combine-yeses` — perf-audit close, speculation README, CLAUDE.md draft stacked for one fast-forward.
- `overnight/2026-05-15-speculation-index` — `index.html` listing the three trips.
- `overnight/2026-05-14-projection-latitude-aware` — `?latfit=1` on the renderer.
- `overnight/2026-05-14-perf-audit-close`, `overnight/2026-05-14-speculation-readme`, `overnight/2026-05-14-claude-md` — the three additive docs.

## Details

- HEAD on `experiment/2026-update`: `bab8203`, two commits ahead of origin before tonight's briefing.
- New branch: `overnight/2026-05-20-reading-b-realized` at `830b1e6`, pushed to origin.
- New file: `_scope_reading_b_signage.html` at repo root, ~230 lines. Single-file, no build step. Style copied from the 05-18 mockups (mono, warm grey panel, red accent, two type sizes).
- Engine links target `_speculation_other_datasets/render.html?dataset=<name>`. Thumb state: `norway-weekend-2013` has 449 thumbs and plays with photos; `bkk-burma-2012` and `summer-2010` have `manifest.js` but 0 thumbs, so they render track-only until thumbs build. The cards say so.
- Twelve overnight branches total: four from 05-14, two from 05-15, two from 05-16, one each from 05-17 / 05-18 / 05-19 / 05-20.
- Last engine code edit: `_speculation_other_datasets/render.html`, 2026-05-16 03:01. Last preservation code change: `experiment-2026/index.html`, commit `ceb97a1`, 2026-05-11.
- No new `TODO` / `FIXME` / `?` markers outside `_archive_2004` in the last seven days. `_Documentation/todo_unfix_bugs.md`: both items remain DONE.
