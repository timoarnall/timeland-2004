# 2026-07-04 — 2004 - Timeland

## Yesterday's calls

- **Q1 (datasets front door).** Menu on its branch, choice still yours: present the folder or keep it a private lab.
- **Q2 (241 vs 1200 density).** Still on the table; the density explorer covers it whenever you want to feel it.
- **Q3 (scope B vs C).** Still yours. The apparatus carries the merge commands for either.
- **Q4 (latitude projection).** Still on the table: ship default-off or drop.

## What's happening

The working branch is quiet; the fresh material is in the speculation folder. Tonight one of its original planning ideas became real: the Moves storyline place check-ins now render as a labelled layer over the Oppdal weekend. The 2013 phone knew the weekend as Home, Oslo Sentralstasjon, Hjerkinn Fjellstue, Snøheim, Snøhetta, and the trail now passes through those names. The candidates plan called this "a fourth element type, new visual vocabulary" back in May; it exists on a branch now, live toggle included.

## Direction and drift

The core piece is done and live at the root, accurate to the original film. The energy stays where it was: the renderer as a portable tool for other trips, and the density question inside the experiment. The place-labels layer deepens the first thread rather than widening the core; it only exists for Moves-era data, so Iceland is untouched by construction.

## Open questions

1. **Do place names join the visual language?** They change the read from pure trail-and-photos to something closer to a diary.
   - Feel it: `git checkout overnight/2026-07-04-place-labels`, serve the folder, open `render.html?dataset=norway-weekend-2013`, toggle "places".
   - Variant to try next: labels are untimed now (a static itinerary); they could instead bloom with the clock like photos do.
2. **Datasets front door: menu or private lab?** The menu is built; this is a presentation call, not a tooling one.
   - A further step either way: play-through video capture, so each trip becomes a watchable clip rather than a live page.
3. **241 curated or 1200 as-shot?** The explorer's sliders answer this faster than argument.
4. **Scope B vs C.** The execution sheet has the commands for whichever reading you pick.
5. **Latitude projection.** Ship default-off or drop; the preview covers Bangkok and Norway.

## Speculative work tonight

- `overnight/2026-07-04-place-labels` — Foursquare check-in labels from the Moves storyline, drawn as screen-constant dots and names with a live "places" checkbox. Same-named stops cluster to one label; overlapping labels thin out when zoomed out. Verified with headless captures at four seek points; datasets without a places file render unchanged. One honest limit: at full zoom-out the camera's width-fit clips the route's north and south ends, so the mountain cluster only names itself once you zoom in. Reversible: `git branch -D` if not useful.
- `overnight/2026-07-04-docs-accuracy` — the speculation folder's planning doc still titled the Oppdal weekend as London in three places; corrected. The handover's redirect-stub bug note described a hash-doubling the committed stubs never had; closed with the check recorded.

## Still on the table

- `overnight/2026-07-03-datasets-index` — the trips menu with strongest-segment deep-links.
- `overnight/2026-05-27-density-explorer` — photos and linger sliders over the experiment.
- The scope apparatus: execution sheet, triptych, launcher, switcher, readings B and C as pages.
- The projection pair: latitude-aware implementation and on/off preview.
- `overnight/2026-05-15-combine-yeses` — perf-close, speculation README, CLAUDE.md draft in one fast-forward.

## Details

- HEAD on `experiment/2026-update`: `bec511f`.
- New branches, both pushed: `overnight/2026-07-04-place-labels` (`4cb1354`, 8 files, 266 insertions), `overnight/2026-07-04-docs-accuracy` (`f0a7ec2`).
- `_speculation_other_datasets/extract_places.py` — new, reads the storyline GPX read-only, writes `norway-weekend-2013/places.js`.
- 11 places from 26 `<wpt>` blocks across `storyline_2013070{5,6,7}.gpx`; the fourteen auto-named Dovrefjell stops cluster to one (5 km radius, label suppression 16 px).
- Toggle default on when `places.js` exists; `?places=0` starts it off; hidden otherwise.
- Verification captures: `_screenshots/norway-weekend-2013_places_seek{030,050,095,100}.png` and `_noplaces_seek095.png`, committed on the branch.
- Source GPX outside the repo (Mapping and Cartography archive), read-only throughout.
- 22 `overnight/*` branches total, all on origin.
